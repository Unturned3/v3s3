
- Create custom FFMPEG package
	Because the "--enable-omx" option in Buildroot's default FFMPEG doesn't actually create a dependency on the cedar_VE package. This can cause a build error when FFMPEG is built before cedar_VE (which provides the OMX sources).

